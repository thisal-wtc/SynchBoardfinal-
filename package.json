{
  "name": "synchboard-monolith",
  "version": "1.0.0",
  "type": "module",
  "description": "Fullstack deployment for SynchBoard",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.0",
    "socket.io": "^4.7.5"
  }
}
